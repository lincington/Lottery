<Window x:Class="LotteryWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        xmlns:lottery ="clr-namespace:CommonModules.LotteryModule;assembly=CommonModules"
        xmlns:local="clr-namespace:LotteryWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="0"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"  x:Name="DF">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="8*"/>
            </Grid.ColumnDefinitions>

            <!-- 导航菜单 -->
            <Border  Grid.Column="0" Background="#f0f0f0">
                <StackPanel >
                    <Button Command="{Binding ShowDashboardCommand}" Content="仪表板" Margin="10"/>

                    <TextBlock Text="模块列表" Margin="10,20,10,5" FontWeight="Bold"/>
                    <ItemsControl ItemsSource="{Binding Modules}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding ModuleName}" 
                                Command="{Binding DataContext.NavigateToModuleCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}"
                                Margin="10,2"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
            <GridSplitter   Grid.Column="1" Width="2" Background="Gray"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <GridSplitter.Triggers>
                    <EventTrigger RoutedEvent="GridSplitter.MouseEnter">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Width" To="5" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                    <EventTrigger RoutedEvent="GridSplitter.MouseLeave">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Width" To="2" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </GridSplitter.Triggers>
              </GridSplitter>
            <!-- 内容区域 -->
            <Border Grid.Column="2">
                <ContentControl  Content="{Binding CurrentModule}" >
                <ContentControl.Resources>
                    <DataTemplate DataType="{x:Type lottery:LotteryCtrlViewModel}">
                        <lottery:LotteryCtrl/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type lottery:LotteryRealCtrlViewModel}">
                        <lottery:LotteryRealCtrl/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type lottery:LotteryReactiveCtrlViewModel}">
                        <lottery:LotteryReactiveCtrl/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type lottery:LotteryHistoryCtrlViewModel}">
                        <lottery:LotteryHistoryCtrl/>
                    </DataTemplate>

                        <DataTemplate DataType="{x:Type lottery:LotteryBuleCtrlViewModel}">
                            <lottery:LotteryBuleCtrl/>
                        </DataTemplate>

                        <DataTemplate DataType="{x:Type lottery:LotteryRedCtrlViewModel}">
                            <lottery:LotteryRedCtrl/>
                        </DataTemplate>
                    </ContentControl.Resources>
            </ContentControl>
            </Border>
        </Grid>
    </Grid>
</Window>
